namespace TERMINAL_FREQUENCY.Visualization
{
    /// <summary>
    /// Dictates where a visualization starts (by reference of Console window or Border), and in some cases where it ends.
    /// </summary>
    /// <see cref="IVisualization"/>
    public enum VisualizationOrigin
    {
        Top,
        Bottom,
        Left,
        Right,
        Center
    }
}
